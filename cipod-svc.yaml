apiVersion: v1
Kind: Pod
metadata:
  name: mypod
  labels:
    app: myapp
spec :
  type : nodeport
  selector :
    app : myapp
  Ports :
  - name : nginx-Port
    protocol : TCP
    port: 80
    targetPort : 80