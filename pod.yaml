apiVersion : v1
kind : Pod
metadata :
  name : mypod
  labels :
    app : myapp
spec :
  containers :
  - image : nginx:latest
    container-name : nginx-container
    Ports :
      - name : nginx-Port
      containerPort : 80